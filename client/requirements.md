## Packages
use-sound | React hook for playing sound effects
framer-motion | For smooth timer animations and page transitions
lucide-react | Icons (already in base but confirming)
date-fns | For formatting dates in history

## Notes
Bell sound URL: https://assets.mixkit.co/active_storage/sfx/2869/2869-preview.mp3 (or similar reliable source)
Backend expects POST /api/sessions with { frequency: number, intervalSeconds: number }
